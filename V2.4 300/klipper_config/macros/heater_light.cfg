[gcode_macro HEATER_LIGHT_ON]
gcode:
	{% for i in range(100) %}
			g4 p15
			set_pin pin=heater_light value={i}
	{% endfor %}


[gcode_macro HEATER_LIGHT_OFF]
gcode:
	{% for i in range(100) %}
			g4 p15
			set_pin pin=heater_light value={100-i|int}
	{% endfor %}